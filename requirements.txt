# Video Duplicate Scanner CLI Dependencies
# Python 3.12+ required (checked at runtime)

# CLI Framework
click>=8.1.0

# Export formats
PyYAML>=6.0

# Fuzzy string matching
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.12.0  # For better performance
# RapidFuzz is a faster, well-maintained alternative to fuzzywuzzy
rapidfuzz>=2.15.0

# Progress bars for CLI long-running operations and for tests
tqdm>=4.65.0

# Windows OneDrive integration (MVP)
# Uses Python standard library: ctypes for Windows API access
# No additional dependencies required for local file detection

# Development dependencies
pytest>=7.0.0
pytest-mock>=3.10.0
pytest-cov>=4.0.0
# Ensure typing backports for older Python compat in some environments
typing_extensions>=4.7.1

# Code quality
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0
jsonschema>=4.0.0
dependency-injector>=4.40.0

# Rich for improved console rendering and integrated logging/progress
rich>=13.0.0
typer>=0.10.0
platformdirs>=2.6.0
readchar>=4.0.0
truststore>=0.10.4